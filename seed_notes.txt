-- Seed Admin User (Username: admin, Password: password123)
-- Seed Center User (Username: center, Password: password123)
-- Note: Passwords must be hashed in a real app. 
-- Since we are using bcrypt in the backend, we cannot insert plain text passwords directly if the backend expects hash.
-- However, for the purpose of 'username and pass', the user needs users to exist.
-- Changing logic: I will Create a quick script to Insert these users via the Backend Registration logic or just Insert them manually if I knew the hash.
-- Let's use a known hash for 'password123': $2b$10$3euPcmQFCiblsZeEu5s7p.9/1/.1/.1/.1 (Example)
-- Actually, it's safer to just provide the user with the ability to register them or I assume they exist.
-- Let's use a generic hash for 'password123' generated by bcrypt. output of bcrypt.hashSync('password123', 10) -> $2b$10$5.9...
-- Valid hash for 'password123': $2b$10$InputYourOwnHashHere

-- Let's assume the user can Register an Admin via the Register page? No, Register is for 'User'.
-- I will create a Node script to seed them properly.

-- Seed SQL for reference (if manual entry needed)
-- INSERT INTO tbl_users (name, email, password_hash, role) VALUES ('Admin', 'admin@sortsense.com', '$2b$10$................', 'ADMIN');
